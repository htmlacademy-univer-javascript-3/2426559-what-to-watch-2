export type FilmCardData = {
    description: string,
    director: string,
    genre: string,
    id: string,
    numberOfRatings: number,
    poster: string,
    preview: string,
    raiting: number,
    starring: string[],
    title: string,
    videoSrc: string,
    year: number
  }

export type PlayerProps = {
    src: string
  }

